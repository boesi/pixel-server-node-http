{
	"name": "Node-Server for Pixel-Mover",
	"version": "0.1",
	"description": "The most simple implementation of a server for pixel-mover",
	"author": "Alexander Bösecke",
	"licence": "GPL",

	"scripts": {
		"dev": "node --inspect=0.0.0.0 --watch --watch-preserve-output app/app.mjs",
		"start": "node app/app.mjs",
		"test": "echo \"Error: no test specified\""
	},

	"dependencies": {
	},

	"devDependencies": {
	}
}
